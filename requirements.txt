# Core dependencies
python >= 3.9

# Data handling
numpy >= 1.21.0
pandas >= 1.3.0

# Async support
asyncio
aiohttp >= 3.8.0

# Market data providers (free APIs)
yfinance >= 0.2.0  # Yahoo Finance - free stock data

# TUI Dashboard
rich >= 13.0.0  # Terminal UI framework

# Optional: For enhanced data collection
# cryptofeed >= 1.0.0  # Cryptocurrency exchange data feeds (self-hostable)
# hummingbot >= 1.0.0  # Self-hostable trading bot framework

# Development dependencies
pytest >= 7.0.0
pytest-asyncio >= 0.20.0

# Web Dashboard
flask >= 2.3.0
